{
  "name": "paid-landing-pages",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/enforce-noindex.mjs",
    "preview": "astro preview",
    "test": "node --test \"test/**/*.test.mjs\""
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "dependencies": {
    "astro": "^7.2.0"
  }
}
